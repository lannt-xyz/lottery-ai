version: '3'
services:
  craw:
    build:
      context: .
      dockerfile: Craw/CrawDockerfile
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ./.env:/app/.env:ro
      - ./models:/app/models
      - ./crawing-data:/app/crawing-data
      - ./data:/app/data
    networks:
      - craw
    environment:
      - CRAWING_TARGET=XSBD,Vietlot655
      - CRAWING_START_DATE=2019-06-03

networks:
  craw:
    driver: bridge
